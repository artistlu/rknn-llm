apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: chat-ds
  labels:
    app: chat-ds
spec:
  selector:
    matchLabels:
      app: chat-ds
  template:
    metadata:
      labels:
        app: chat-ds
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: ai-app
                    operator: In
                    values:
                      - model-chat
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: chat-ds
          image: fxzscheck-1385124d.ecis.huabei-3.cmecloud.cn/kirnu/chat:v1.2
          securityContext:
            privileged: true
          ports:
            - containerPort: 8089
          volumeMounts:
            - name: model-volume
              mountPath: /app/rkllm_model
      volumes:
        - name: model-volume
          hostPath:
            path: /nasroot/rkllm_model
---
apiVersion: v1
kind: Service
metadata:
  name: chat-svc
spec:
  type: NodePort
  selector:
    app: chat-ds
  ports:
    - port: 8099
      targetPort: 8089
      nodePort: 30099